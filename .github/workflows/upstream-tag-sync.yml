name: Build & Push Docker Only on Upstream Change

on:
  schedule:
    - cron: '0 * * * *' # stündlich prüfen
  workflow_dispatch:

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Add and Fetch Upstream
        run: |
          git remote add upstream https://github.com/NMminer1024/NMController_web.git
          git fetch upstream

      - name: Debugging SHA-Werte
        run: |
          LOCAL_SHA=$(git rev-parse origin/main)
          UPSTREAM_SHA=$(git rev-parse upstream/main)
          echo "LOCAL_SHA=$LOCAL_SHA"
          echo "UPSTREAM_SHA=$UPSTREAM_SHA"

      - name: Compare Upstream with Origin
        id: compare
        run: |
          LOCAL_SHA=$(git rev-parse origin/main)
          UPSTREAM_SHA=$(git rev-parse upstream/main)
          echo "local_sha=$LOCAL_SHA" >> $GITHUB_ENV
          echo "upstream_sha=$UPSTREAM_SHA" >> $GITHUB_ENV
          if [[ "$LOCAL_SHA" != "$UPSTREAM_SHA" ]]; then
            echo "update_required=true" >> $GITHUB_ENV
          else
            echo "update_required=false" >> $GITHUB_ENV
          fi

      - name: Exit if No Update Needed
        if: env.update_required == 'false'
        run: |
          echo "No changes in upstream. Skipping build."
          exit 0

      - name: Merge Upstream into Fork
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git merge upstream/main --allow-unrelated-histories --no-edit || echo "Merge failed"
          git push origin main

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: fgrfn/nmcontroller_web:latest
